# games/

Games live in their own repositories. Clone one here (`git clone <repo> games/<name>`) to work on it with the engine: its
`tsconfig.json` and `.mcp.json` reach the engine packages through `../../packages` and `../../apps/cli`. Everything in
this folder except this README is ignored by the engine repo.
