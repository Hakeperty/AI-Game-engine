import { withDefaults } from 'aige/model';
import base from './templates/blob-character.model.ts';

/** blob: variant of the 'blob-character' template. */
export default withDefaults(base, {}, { name: 'blob' });
